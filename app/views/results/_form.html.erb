<%= form_for @result, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :match_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :match_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :result1, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :result1, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :result2, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :result2, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                results_path, :class => 'btn' %>
  </div>
<% end %>
