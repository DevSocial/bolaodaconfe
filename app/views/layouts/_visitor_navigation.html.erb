<%= link_to "Bolão da Confe", root_path, :class => 'brand' %>
<ul class="nav">
  <li>
  <%= link_to 'Registre-se', new_user_path %>
  </li>
  <li>
  <%= link_to 'Logar', new_user_sessions_path %>
  </li>
  
  <%= form_for :user, :html => { :class => 'form-inline'}, :remote => true do |u| %>
    <%= u.text_field :email, :class => 'input-small', :placeholder => 'Email' %>
    <%= u.password_field :password, :class => 'input-small', :placeholder => 'Password' %>
    <%= u.submit 'Entrar', :class => 'btn' %>
  <% end %>
  
</ul>
